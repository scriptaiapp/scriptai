
services:
  redis:
    image: redis:latest
    command: redis-server --requirepass your_strong_password_here
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data: